{#
  Wrapperless region template.
  This renders only the region content without any surrounding markup.
  It is enabled for specific regions via theme suggestions in gravelle1.theme.
  To use for another region, add its machine name to $bare_regions in
  gravelle1_theme_suggestions_region_alter().
#}
{% if content %}
  {{ content }}
{% endif %}

