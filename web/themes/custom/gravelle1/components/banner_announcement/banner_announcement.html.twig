{#
  Banner Announcement SDC
  Expects props: has_label, label, text (HTML), url, link_title, image (HTML)
  Renders full block structure via a per-view-mode partial.
#}

{{ attach_library('gravelle1/banner_announcement') }}

{% set vm = props.view_mode|default('default') %}
{{ include([
  '@gravelle1/components/banner_announcement/partials/' ~ vm ~ '.html.twig',
  '@gravelle1/components/banner_announcement/partials/default.html.twig'
], { props: props }) }}
